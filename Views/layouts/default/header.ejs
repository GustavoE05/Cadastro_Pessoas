<header>
    <div>
        <h2> CADASTRAR PESSOA </h2>
    <div>
        <% if (locals.layoutsVariables.user) { %>
            <p>
                Olá, <%- locals.layoutsVariables.user.nome %>
                <a href="/logout" class="btn-primary">
                    <i class="bi bi-box-arrow-in-left"></i> logout
                </a>                
            </p>
        <% } %>
    </div>
</header>
